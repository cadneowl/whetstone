# Python
__pycache__/
*.py[cod]
*.egg-info/
.Python
build/
dist/

# Virtualenv (managed by uv; recreate with `uv sync`)
.venv/

# Run records + derived index (telemetry, not source of truth — safe to delete)
.whetstone/

# Console frontend: built assets are generated at packaging time, never committed
node_modules/
src/whetstone/ui/static/
ui/openapi.json
.playwright-mcp/

# Tooling caches
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Local env / secrets
.env
.env.*
# ...except the committed template, which holds names and no values.
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
