# Generated by `npm run gen:api` from the FastAPI schema. Formatting it produces a diff that the
# next regeneration throws away, so the two tools would fight forever over a file neither owns.
src/api/schema.d.ts
openapi.json

# Build output, inside the Python package.
../src/whetstone/ui/static/
node_modules/
